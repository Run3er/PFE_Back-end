ext {
    // TODO: make use of (globalize) following
    springBootVersion = '1.5.2.RELEASE'
}

subprojects {
    apply plugin: 'java'

    jar {
        baseName = 'proj-mngmnt-saas'
    }

    sourceCompatibility = 1.8
    targetCompatibility = 1.8
}